# Optional overrides for local Docker Compose.
# You can run `docker compose up --build` without this file.

# Database
POSTGRES_PASSWORD=admin123

# pgAdmin login
PGADMIN_DEFAULT_EMAIL=admin@nida.com
PGADMIN_DEFAULT_PASSWORD=admin123

# Backend app secret
SECRET_KEY=change-this-dev-secret

# Optional app mode
DEFAULT_DATA_MODE=real
