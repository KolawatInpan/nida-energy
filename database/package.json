{
  "name": "nida-database",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "prisma": "^6.18.0"
  },
  "scripts": {
    "generate": "prisma generate --schema=./schema.prisma",
    "studio": "prisma studio --schema=./schema.prisma --port 5555 --hostname 0.0.0.0"
  }
}
